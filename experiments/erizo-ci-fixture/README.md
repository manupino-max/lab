# ERIZO CI Fixture

Public CI fixture derived from the repository's synthetic ERIZO/D0.3 execution pattern.

Purpose: exercise the same GitHub Actions mechanics used by the private research repository without publishing private governance, publication plans, or thesis-only evidence.

The fixture uses only synthetic data generated at runtime and a deliberately reduced parameter grid so CI remains fast.
