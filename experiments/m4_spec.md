# M4

Primary intervention: z'_g = z + alpha (m_lambda - mu_g), with m_lambda = lambda mu0 + (1-lambda) mu1. Primary lambda=.5 midpoint. Alpha sweep 0..1. Utility and fairness are evaluated after refitting probes/task models on the transformed training representation. Diagonal Gaussian symmetric KL is a secondary diagnostic, not a proof of distributional equality.
